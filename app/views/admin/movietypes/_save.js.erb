$("ul.errors").html("")
<% if @movietype.errors.any? %>
  <% @movietype.errors.full_messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.html_safe %>"))
  <% end %>
<% else %>
  $("#product-modal").modal("hide")
<% end %>