$(".errors").html("")
<% if @schedule_time.errors.any? %>
  <% @schedule_time.errors.full_messages.each do |message| %>
    $(".errors").html("<%= message.html_safe %>")
  <% end %>
<% else %>
  $("#product-modal").modal("hide")
<% end %>